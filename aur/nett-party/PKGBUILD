pkgname=nett-party
pkgver=0.1.3
pkgrel=1
pkgdesc="Another Mihomo GUI."
arch=('x86_64' 'aarch64')
url="https://github.com/nett-party-org/nett-party"
license=('GPL3')
conflicts=("$pkgname-git" "$pkgname-bin" "$pkgname-electron" "$pkgname-electron-bin")
depends=('gtk3' 'libnotify' 'nss' 'libxss' 'libxtst' 'xdg-utils' 'at-spi2-core' 'util-linux-libs' 'libsecret')
optdepends=('libappindicator-gtk3: Allow nett-party to extend a menu via Ayatana indicators in Unity, KDE or Systray (GTK+ 3 library).')
makedepends=('nodejs' 'pnpm' 'libxcrypt-compat')
install=$pkgname.install
source=(
    "${pkgname}-${pkgver}.tar.gz::${url}/archive/refs/tags/v${pkgver}.tar.gz"
    "${pkgname}.sh"
)
sha256sums=("52d761e9432e17477acb8adb5744676df946476e0eb5210fee2b6d45f497f218"
"242609b1259e999e944b7187f4c03aacba8134a7671ff3a50e2e246b4c4eff48")
options=('!lto')

prepare(){
    cd $srcdir/nett-party-${pkgver}
    sed -i "s/productName: nett-party/productName: nett-party/" electron-builder.yml
    pnpm install
}

build(){
    cd $srcdir/nett-party-${pkgver}
    pnpm build:linux deb
}

package() {
	cd $srcdir/nett-party-${pkgver}/dist
    bsdtar -xf nett-party-linux-${pkgver}*.deb
    bsdtar -xf data.tar.xz -C "${pkgdir}/"
    chmod +x ${pkgdir}/opt/nett-party/nett-party
    chmod +sx ${pkgdir}/opt/nett-party/resources/sidecar/mihomo
    chmod +sx ${pkgdir}/opt/nett-party/resources/sidecar/mihomo-alpha
    chmod +sx ${pkgdir}/opt/nett-party/resources/sidecar/mihomo-smart
    install -Dm755 "${srcdir}/../${pkgname}.sh" "${pkgdir}/usr/bin/${pkgname}"
    sed -i '3s!/opt/nett-party/nett-party!nett-party!' "${pkgdir}/usr/share/applications/${pkgname}.desktop"

    chown -R root:root ${pkgdir}
}
